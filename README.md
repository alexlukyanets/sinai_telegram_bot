## For docker build use - docker build -t sinai_bot .
## For run docker - docker run -it --rm -p 80:8000 gen_art
## For run local run - docker run -it --rm -p 80:8000 gen_art
## For run local run - python -m telegram_bot.bot

## Python 3.11